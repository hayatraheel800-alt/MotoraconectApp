import React,{useState} from "react"; import {View,TextInput,StyleSheet,Alert} from "react-native"; import {SafeAreaView} from "react-native-safe-area-context"; import {Link,useRouter} from "expo-router"; import {useTheme} from "@/theme/ThemeProvider"; import {AppText} from "@/components/AppText"; import {Button} from "@/components/Button"; import {signUp} from "@/lib/auth";
export default function SignupScreen(){const theme=useTheme(),router=useRouter();const[name,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[loading,setLoading]=useState(false);async function submit(){setLoading(true);const{data,error}=await signUp(name,email,password);setLoading(false);if(error){Alert.alert("Signup failed",error.message);return}if(data.session)router.replace("/(tabs)");else Alert.alert("Check your email","Your account was created. Verify your email before signing in.")}return <SafeAreaView style={[styles.container,{backgroundColor:theme.colors.background}]}><View style={{padding:theme.spacing.lg}}><AppText variant="display">Create account</AppText><AppText variant="body" color={theme.colors.textSecondary} style={{marginBottom:theme.spacing.lg}}>Join Motoraconect</AppText><TextInput placeholder="Full name" value={name} onChangeText={setName} style={[styles.input,{borderColor:theme.colors.border}]}/><TextInput placeholder="Email" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail} style={[styles.input,{borderColor:theme.colors.border,marginTop:theme.spacing.sm}]}/><TextInput placeholder="Password" secureTextEntry value={password} onChangeText={setPassword} style={[styles.input,{borderColor:theme.colors.border,marginTop:theme.spacing.sm}]}/><View style={{marginTop:theme.spacing.lg}}><Button label="Create account" loading={loading} onPress={submit}/></View><Link href="/(auth)/login" style={{marginTop:theme.spacing.lg}}><AppText variant="body" color={theme.colors.info}>Already have an account? Log in</AppText></Link></View></SafeAreaView>} const styles=StyleSheet.create({container:{flex:1},input:{borderWidth:1,borderRadius:10,paddingHorizontal:14,paddingVertical:12,fontSize:16}});